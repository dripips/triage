<% content_for :title, @user.display_name %>

<header class="page-header">
  <div>
    <div class="page-header__eyebrow"><%= link_to "← #{t('navigation.team', default: 'Команда')}", users_path %></div>
    <h1 class="page-header__title m-0"><%= @user.display_name %></h1>
    <p class="page-header__subtitle m-0"><%= @user.email %> · <%= @user.role_label %></p>
  </div>
</header>

<%= render "form", user: @user %>
