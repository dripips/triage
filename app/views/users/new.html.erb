<% content_for :title, t("users.new", default: "Новый сотрудник") %>

<header class="page-header">
  <div>
    <div class="page-header__eyebrow"><%= link_to "← #{t('navigation.team', default: 'Команда')}", users_path %></div>
    <h1 class="page-header__title m-0"><%= t("users.new", default: "Новый сотрудник") %></h1>
  </div>
</header>

<%= render "form", user: @user %>
