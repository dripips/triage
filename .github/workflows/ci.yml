name: CI

on:
  pull_request:
  push:
    branches: [master]

jobs:
  scan_ruby:
    name: Security scan (Ruby)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Brakeman (Rails static analysis)
        run: bundle exec brakeman --no-pager

      - name: Bundler audit (gem CVEs)
        run: bundle exec bundler-audit

  scan_js:
    name: Security scan (JavaScript)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Importmap audit
        run: bundle exec rails importmap:audit

  lint:
    name: Lint (Rubocop)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Run Rubocop
        run: bundle exec rubocop --no-color

  test:
    name: Tests (RSpec)
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

    env:
      RAILS_ENV: test
      TRIAGE_DATABASE_HOST: localhost
      TRIAGE_DATABASE_PORT: "5432"
      TRIAGE_DATABASE_USERNAME: postgres
      TRIAGE_DATABASE_PASSWORD: postgres

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Prepare database
        run: bundle exec rails db:test:prepare

      - name: Zeitwerk autoload check (catches class-naming mismatches)
        run: bundle exec rails zeitwerk:check

      - name: Run RSpec (incl. smoke spec)
        run: bundle exec rspec --format documentation
